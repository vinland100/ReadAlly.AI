# Backend Configuration
DASHSCOPE_API_KEY=

# Frontend Configuration
# For Docker local dev (browser access), this should be http://localhost:8000
# For Production, leave empty to use relative path (if served on same domain) or set to specific domain
NEXT_PUBLIC_API_URL=http://localhost:8000