# Backend Configuration
DASHSCOPE_API_KEY=

# Frontend Configuration
# If running locally with Docker Compose, this likely can be empty to use the Relative URL or configured to localhost.
# For production, set to the actual domain.
NEXT_PUBLIC_API_URL=
