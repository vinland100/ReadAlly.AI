# For multiple domains behind the same reverse proxy (Caddy), 
# leave this EMPTY to use relative URLs (e.g., /api/...).
# Locally, keep it commented out or unset to use http://localhost:8000
NEXT_PUBLIC_API_URL=
